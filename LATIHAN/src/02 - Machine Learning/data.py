import numpy as np

a = np.array([1,2,3,4,5])
b = np.array([6,7,8,9,0])
c = np.array([1,3,5,7,9])
d = np.array([2,4,6,8,0])
e = np.array(([10,11,12], [13,14,15]))
f = np.array(([16,17,18], [-1,-2,-3]))
g = np.arange(1,10,2) # Vector dengan range
h = np.array([1.5, 2.5, 3.14, 4.5, 5.2])
i = np.linspace(1,10,4) # Vector linspace
j = np.array([(1,2,3) , (4,5,6)]) # Array multidimensi / matriks
k = np.zeros((5,5)) # Matriks bernilai nol / zeros
l = np.ones((5,5)) # Matriks bernilai satu / ones
m = np.identity(5) # Membuat matriks identitas
n = np.eye(5) # Membuat matriks identitas



